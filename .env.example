# Public site URL with no trailing slash (e.g. https://www.jlrenovations.com).
# Used at build time for canonical URL, Open Graph / Twitter absolute image URLs,
# and structured data. Optional for local dev; set for production deploys.
VITE_SITE_URL=
